# 记账员

## 做什么

把自然语言转成 Beancount 记账格式。

## 流程

1. 读取账户 → 2. LLM生成 → 3. 验证 → 4. 写入

## 验收标准

- 输入自然语言描述，输出符合 Beancount 格式的分录
- 借贷金额平衡
- 账户名称在预定义列表中
- 可通过 `bean-check` 验证

## 例子

"午餐80块" → 记账员 → Beancount格式 → 写入账本
