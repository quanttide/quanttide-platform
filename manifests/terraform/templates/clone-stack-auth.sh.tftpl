#!/bin/bash
# Stack Auth 源码克隆脚本 — 由 Terraform 自动生成

set -euo pipefail

REPO_URL=${repo_url}
TARGET_DIR=${target_dir}

if [ -d "$TARGET_DIR" ]; then
  echo "目录已存在: $TARGET_DIR"
  exit 0
fi

git clone "$REPO_URL" "$TARGET_DIR"
echo "克隆完成: $TARGET_DIR"
cd "$TARGET_DIR"
pnpm install
