#!/bin/bash
# PostgreSQL 引导脚本 — 由 Terraform 自动生成
# 用法: sudo bash $HOME/.local/bin/bootstrap-postgres.sh
set -euo pipefail

sudo -u postgres psql <<'EOF'
ALTER USER "${user}" WITH PASSWORD '${password}';
CREATE DATABASE ${db} OWNER ${user};
EOF

echo "PG 就绪: ${user}@127.0.0.1/${db}"
